{
  "name": "school-vote",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A Paystack-powered school leadership voting application.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1"
  }
}
